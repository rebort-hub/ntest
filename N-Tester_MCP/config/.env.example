# N-Tester MCP 服务配置

# N-Tester Tools - 后端 API 地址
# Docker 环境使用: http://backend:8018
# 本地开发使用: http://localhost:8018
N-Tester_BACKEND_URL=http://localhost:8018

# N-Tester Tools - API Key
# 默认使用系统的固定密钥，直接使用即可

# ⚠️ 安全提示：
# - 开发环境：直接使用下面的默认密钥即可
# - 生产环境：务必更换为新的安全密钥
# 创建新密钥：登录后台 -> AI驱动生成管理 -> API密钥管理
N-Tester_API_KEY=N-Tester-default-mcp-key-2025

# ==================== Playwright 集成配置 ====================

# 是否启用 Playwright 集成（调用官方 Playwright MCP）
# true: 启用增强功能，调用官方 Playwright MCP
# false: 仅使用内置 Playwright 功能
ENABLE_PLAYWRIGHT_INTEGRATION=true

# Playwright MCP HTTP 地址
# 需要先启动 Playwright MCP HTTP 桥接服务
# 启动命令: node start-playwright-mcp-http-bridge.js
PLAYWRIGHT_HTTP_URL=http://127.0.0.1:3000

# 是否启用增强工具（在官方工具基础上添加额外功能）
ENABLE_ENHANCED_TOOLS=true

# 是否启用导航历史记录
ENABLE_NAVIGATION_HISTORY=true

# 是否自动保存截图到数据库
AUTO_SAVE_SCREENSHOTS=false

# 工具调用超时时间（秒）
TOOL_CALL_TIMEOUT=60

# 日志级别: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# MS MCP API - N-Tester测试平台集成配置（可选）
# 如需使用 MS N-Tester测试平台集成，请取消注释并填写正确的密钥
# 注意：MS_SECRET_KEY 必须是 16、24 或 32 字节长度
# MS_API_HOST=http://ms.example.com
# MS_ACCESS_KEY=your_16byte_key1
# MS_SECRET_KEY=your_16byte_key2